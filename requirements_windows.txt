# Windows Test Requirements
# Minimal requirements for testing the day planner on Windows

# Core dependencies
PyYAML>=6.0
pytz>=2023.3
APScheduler>=3.10.1

# For Windows GUI (included with Python)
# tkinter is included with Python installation

# Optional: For better date handling
python-dateutil>=2.8.2 